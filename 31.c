#include<stdio.h>
#include<string.h>
int main()
{
    char a[100];
    int i,flag=0;
    gets(a);
    for(i=0;a[i]!='\0';i++)
    {
    if(s[i]!=' ')
    {
        flagt++;
    }
   
    }
   printf("%d",flag);
   return 0;
}